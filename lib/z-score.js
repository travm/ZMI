function calculateZscore() {

    // X = BMI? Weight?
    // Z = ln(X/M)/S, L=0

    if(L != 0) {

        // Z = ((X/M)**L - 1 / LS

    } else {

        // Z = ln(X/M)/S ,L=0

    }

}